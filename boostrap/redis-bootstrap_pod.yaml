apiVersion: v1
kind: Pod
metadata:
  name: redis-bootstrap
  labels:
    app: redis
    tier: redis-sentinel
spec:
  containers:
    - name: master
      image: redis:v5
      ports:
       - containerPort: 6379
      env:
        - name: MASTER
          value: "true"
      volumeMounts:
        - mountPath: /redis-master-data
          name: data
    - name: sentinel
      image: redis:v5
      env:
        - name: SENTINEL
          value: "true"
      ports:
        - containerPort: 26379 
  volumes:
    - name: data
      emptyDir: {}
